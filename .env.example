# =============================================================================
# SaaSRandevu — Ortam Değişkenleri (Environment Variables)
# .env.example dosyasını .env olarak kopyalayıp değerleri doldurun.
# Hassas değerleri asla repoya commit etmeyin.
# =============================================================================

# --- Admin panel ---
# Giriş şifresi (en az 8 karakter)
ADMIN_PASSWORD=your-secure-password
# JWT session imzası (en az 32 karakter; örn: openssl rand -base64 32)
ADMIN_SESSION_SECRET=generate-with-openssl-rand-base64-32

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Service role key — backend işlemleri (RLS bypass); gizli tutun
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- Upstash Redis ---
# Oturum (session), telefon–tenant eşlemesi ve rate limit için
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token

# --- OpenAI ---
OPENAI_API_KEY=sk-xxx

# --- WhatsApp (Meta Cloud API) ---
# Webhook doğrulama (GET hub.verify_token)
WHATSAPP_VERIFY_TOKEN=your-verify-token
# Webhook imza doğrulama (POST x-hub-signature-256); Meta App Dashboard'dan alın
WHATSAPP_WEBHOOK_SECRET=your-app-webhook-secret
WHATSAPP_PHONE_NUMBER_ID=xxx
WHATSAPP_ACCESS_TOKEN=xxx
# wa.me link ve generateTenantAssets için
WHATSAPP_PHONE_NUMBER=905551234567
# generateTenantAssets için opsiyonel (yoksa WHATSAPP_PHONE_NUMBER kullanılır)
WHATSAPP_API_PHONE=905551234567

# --- Cron (hatırlatma / review reminder) ---
# Cron isteklerini doğrulamak için gizli anahtar
CRON_SECRET=your-cron-secret

# --- Uygulama URL ---
# Webhook cevapları, link üretimi, işletme listesi linki
NEXT_PUBLIC_APP_URL=https://your-domain.com
